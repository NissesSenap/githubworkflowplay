name: Trigger xenit-home to regenerate docs
on:
  push:
    branches:
      - main

env:
  ENVIRONMENTS: "env1"

jobs:
  call-workflow-1:
    uses: NissesSenap/githubworkflowplay/.github/workflows/workflow.yaml@f33c36953fe219e1669935b6ac5466117bc01fd4
    with:
      username: mona
      dir: dir1
      test: "true"
      ENVIRONMENTS: |
        {
          "environments":[
            {"name":"dev"},
          ]
        }
    secrets:
      DATA: ${{ secrets.DATA }}
