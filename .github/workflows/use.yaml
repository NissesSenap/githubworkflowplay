name: Trigger xenit-home to regenerate docs
on:
  push:
    branches:
      - main

env:
  ENVIRONMENTS: "env1"

jobs:
  call-workflow-1:
    uses: NissesSenap/githubworkflowplay/.github/workflows/workflow.yaml@22faafcfb1a42c765fbaf415fea5917fdbb725ff
    with:
      username: mona
      dir: dir1
      test: true
