name: Trigger xenit-home to regenerate docs
on:
  push:
    branches:
      - main

env:
  ENVIRONMENTS: "env1"

jobs:
  call-workflow-1:
    uses: NissesSenap/githubworkflowplay/.github/workflows/workflow.yaml@1c9467936ab9c19e8175181e80e23241cfe25ea2
    with:
      username: mona
      dir: dir1
      test: "true"
    secrets:
      DATA: ${{ secrets.DATA }}
